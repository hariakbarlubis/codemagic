version: 2.0
workflows:
  ios-workflow:
    name: iOS Workflow
    environment:
      flutter: stable
    triggers:
      push:
        branches:
          only:
            - main
    scripts:
      - name: Build iOS app
        script: |
          flutter build ios
